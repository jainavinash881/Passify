import React from 'react';
import ReactDOM from 'react-dom/client';
import PasswordManager from './PasswordManager.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PasswordManager />
  </React.StrictMode>
);
